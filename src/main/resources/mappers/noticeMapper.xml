<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="noticeMapper">
   
  	<select id="getNum" resultType="java.lang.Integer">
  	select board_seq.nextval from dual
  		</select>
  
  
  	<update id="hitUpdate" parameterType="java.lang.Integer">
  		update notice set 
  		hit=hit+1 where num=#{num}
  	</update>
  	
  	<select id="totalCount" resultType="java.lang.Integer">
  		select nvl(count(num), 0) from notice
  		where title like #{search}
  	</select>
  
  
  	<select id="selectList" parameterType="com.iu.util.RowNum" resultType="com.iu.notice.NoticeDTO">
  		select * from
  		(select rownum R, N.* from
  		(select * from notice
  		<if test="kind == 'title'">
  		 where title like '%'||#{search}||'%' 
  		 
  		 </if>
  		 <if test="kind ==writer">
  		 where writer like '%'||#{search}||'%'
  		 </if>
  		 
  		 <if test="kind==contents">
  		 where contents like '%'||#{search}||'%'
  		 </if>
  		 order by num desc)N)
  		where R between #{startRow} and #{lastRow}
  	</select>
  	
  	<select id="selectOne" parameterType="java.lang.Integer" resultType="com.iu.notice.NoticeDTO">
  		select * from notice where num=#{num}
  	</select>
  	
  	<insert id="insert" parameterType="com.iu.board.BoardDTO" >
  	<selectKey keyProperty="num" resultType="int" order="BEFORE">
  		select board_seq.nextval from dual
  		<!-- select max(num)+1 from notice -->
  	</selectKey>
  		insert into notice values (
  		#{num}, 
  		#{writer},
  		#{title},
  		#{contents},
  		sysdate,0)
  	</insert>
  	
  	<delete id="delete" parameterType="java.lang.Integer">
  		delete notice where num=#{num}
  	</delete>
  	
  	<update id="update" parameterType="com.iu.board.BoardDTO"> 
  		update notice set title=#{title}, contents=#{contents} where num=#{num}
  	
  	</update>
  
  </mapper>